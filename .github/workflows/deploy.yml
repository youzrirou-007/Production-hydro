name: Deploy to Firebase

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build site
        run: npm run build

      - name: Deploy to Firebase
        run: |
          npx firebase-tools deploy --token "1//03aq0W_jdKmKgCgYIARAAGAMSNwF-L9IrpzKnnYsiylteIBg1XOwu72mv6Sh3ISNV5Iv6OpbFW5t_ZYCIASw6HUMFDIZTHQkH4hA" --project production-hydromines
