name: Deploy to Firebase

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build site
        run: npm run build

      - name: Deploy to Firebase
        run: |
          npx firebase-tools deploy --token "${{ secrets.FIREBASE_TOKEN }}" --project production-hydromines
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
